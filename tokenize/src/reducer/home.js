const initState = {
  bidList: ['0.33333', '0.05298'],
}

const todoReducer = (state = initState, action) => {
  switch (action.type) {
    default:
      return state;
  }
}

export default todoReducer
